# SQLite 数据库文件路径（相对项目根目录）
DATABASE_URL="file:./dev.db"

# Auth.js 会话密钥，生成方式：openssl rand -base64 32
AUTH_SECRET=""

# 生产环境设置为 true，允许信任反向代理转发
AUTH_TRUST_HOST="true"

# 后台入口路径，建议改成不易猜测的值（默认 kb-9f3x）
ADMIN_BASE_PATH="kb-9f3x"
